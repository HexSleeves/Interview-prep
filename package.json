{
  "name": "interview-prep",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "cli": "bun run src/cli.ts",
    "list": "bun run src/cli.ts list",
    "check": "bun run src/cli.ts check --all",
    "test:harness": "bun test",
    "test:problems": "bun run src/cli.ts check --all",
    "test": "bun test && bun run src/cli.ts check --all",
    "test:bst": "bun run src/cli.ts check --domain binary-search-tree",
    "test:frequency": "bun run src/cli.ts check --domain frequency",
    "test:prefix": "bun run src/cli.ts check --domain prefix-sum",
    "test:window": "bun run src/cli.ts check --domain sliding-window"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
